import React from 'react';

const AdminCollection = () => {
    return (
    <div>
        <h2>Admin Collection</h2>
        <p>This is the collection page for admins. It may contain features like managing products, editing collections, etc.</p>
    </div>
    );
};

export default AdminCollection;